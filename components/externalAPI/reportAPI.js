const reportAPI = module.exports;

reportAPI.reportExcessPayableDevicesCase = (user) => {
    // Call to some external API to report suspicious or fraud system.
    console.log("Excess number of payable devices case reported")
    //TODO: log!
}